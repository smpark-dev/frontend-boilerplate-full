# 기존 .husky 폴더 삭제
rm -rf .husky

# Husky 9.x 방식으로 다시 초기화
npx husky init

# 간단한 pre-commit 설정 (타입 체크 제외)
echo '#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npx lint-staged' > .husky/pre-commit

# 실행 권한 부여
chmod +x .husky/pre-commit